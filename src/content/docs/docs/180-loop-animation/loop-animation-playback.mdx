---
title: "Loop Animation Playback"
description: "Loop Animation Playback"
slug: "docs/loop-animation-playback"
sidebar:
  order: 20
draft: false
---

# Loop Animation Playback

The **Loop Animation** button in the [Timeline Panel](../../docs/timeline-panel) toolbar makes the animation repeat continuously during preview playback inside the editor.

This setting only affects how the animation plays while you are working — it does not change the exported file or the document's loop settings.

## How to Use

Click **Loop Animation** in the timeline toolbar to toggle it on or off. When active, the playhead returns to the start automatically when it reaches the end of the animation range, so playback continues without interruption.

To loop the exported animation, use the **Loop** setting in [Document Properties](../../docs/manage-document-properties) instead.
